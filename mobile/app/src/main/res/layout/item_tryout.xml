<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardCornerRadius="12dp"
    app:cardElevation="0dp"
    app:cardBackgroundColor="@color/white"
    android:layout_marginBottom="12dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <FrameLayout
            android:id="@+id/icon_container"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@drawable/bg_circle_blue_light"
            android:layout_marginEnd="12dp">
            <!-- Menggunakan ic_menu_my_calendar sebagai pengganti ikon dokumen jika tidak ada, atau tetap ic_menu_edit -->
            <ImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:src="@drawable/ic_materi"
                android:tint="@color/neo_blue"
                android:layout_gravity="center"/>
        </FrameLayout>

        <TextView
            android:id="@+id/tv_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_toEndOf="@id/icon_container"
            android:text="Tryout UTBK - SNBT #1"
            android:textStyle="bold"
            android:textColor="@color/black"
            android:textSize="16sp"/>

        <TextView
            android:id="@+id/tv_subtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/tv_title"
            android:layout_toEndOf="@id/icon_container"
            android:layout_marginTop="4dp"
            android:text="Mulai Tanggal 27/11/2025"
            android:textColor="@color/text_grey"
            android:textSize="12sp"/>

        <!-- Tombol Status / Aksi -->
        <Button
            android:id="@+id/btn_kerjakan"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/icon_container"
            android:layout_marginTop="12dp"
            android:text="Upgrade Premium"
            android:textSize="12sp"
            android:textAllCaps="false"
            android:backgroundTint="#4DD0E1" 
            android:textColor="@color/white"
            android:minHeight="0dp"
            android:paddingVertical="8dp"
            android:paddingHorizontal="20dp"
            android:insetTop="0dp"
            android:insetBottom="0dp"/>

    </RelativeLayout>
</androidx.cardview.widget.CardView>