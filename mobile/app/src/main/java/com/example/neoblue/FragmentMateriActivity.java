package com.example.neoblue;

import androidx.fragment.app.Fragment;

/**
 * This file is deprecated and should be removed.
 * The actual fragment logic resides in HomeFragment.java or MateriFragment.java (if used).
 * Since the user wants to remove "double" files, and MainActivity uses HomeFragment for "Materi",
 * this activity wrapper is likely obsolete.
 * 
 * However, as an AI, I cannot delete files. I can only empty them or mark them as deprecated.
 */
public class FragmentMateriActivity extends Fragment {
   // Empty class to avoid build errors if it's still referenced in Manifest, 
   // though it should ideally be removed from Manifest and disk.
}
